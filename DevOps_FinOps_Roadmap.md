# 🚀 CoverChallenge 데브옵스/FinOps 마스터 플랜 및 컨텍스트 인수인계

> **[Agent Context Handoff (인수인계 완료 상태)]**
> 이 파일은 새로운 채팅 세션에서 AI 에이전트(FinOps 튜터)가 이전 대화의 맥락과 앞으로의 로드맵을 즉시 파악하기 위해 작성된 인수인계 문서입니다.
> - **완료된 상태 (Phase 1~4 개념 훈련 종료):** 로컬 환경 `docker-compose.yml` 및 `Dockerfile` 최적화(레이어 캐싱, 볼륨, 보안망 격리), AWS 스왑 메모리(Thrashing) 방어 논리, Nginx 기반 무중단 배포(Blue/Green) 로직 및 스크립트(`deploy.sh`) 설계까지 완벽하게 소크라테스식 시뮬레이션으로 훈련을 마쳤습니다.
> - **유저의 특성 (매우 중요):** 사용자는 '단순 정답(코드 복붙)'을 극도로 혐오합니다. 항상 "왜 이 기술을 써야 하는가(비용, 보안, 성능)"를 꼬리 질문(압박 면접)으로 묻고, 사용자의 오답을 유도한 뒤 깨달음을 주는 '티키타카 학습법'으로 응대해야 합니다.
> - **다음 액션 플랜:** 이론으로 훈련한 이 파이프라인들을 **실제 AWS EC2와 GitHub Actions 환경에 코드로 직접 연동하고 구동 테스트**를 진행하는 것입니다.

---

## 🗺️ 근본부터 다지는 데브옵스 마스터 로드맵 (진행도)

### [✅ 완료] Phase 1: 운영체제(Linux)와 네트워크의 본질
- `internal` 브릿지 네트워크와 Embedded DNS (127.0.0.11) 원리 체화.
- Nginx 리버스 프록시 패킷 흐름 및 DB(3306) 외부 노출 차단(Ransomware 방어) 원리 이해.

### [✅ 완료] Phase 2: 컨테이너 격리 기술의 딥다이브 (Docker)
- `Dockerfile` 레이어(Layer) 캐싱 최적화로 빌드 속도 단축 로직 완벽 해부.
- 도커 볼륨(Volume)을 통한 데이터 영속성(Persistence) 개념 확보.

### [✅ 완료] Phase 3: 클라우드 인프라와 FinOps 최적화 (AWS)
- 프리티어(RAM 1GB)에서 발생하는 OOM 및 쓰래싱(Thrashing) 병목 현상 인지.
- 인프라 과금을 방어하기 위한 스왑(Swap) 메모리 구축 타당성(FinOps 방어 논리) 확립.

### [✅ 완료] Phase 4: 무중단 배포 및 자동화 (CI/CD) 설계
- 단순 롤링 배포와 Blue/Green 배포의 차이점 완벽 인지.
- 무중단 배포의 핵심인 **Health Check**와 **Graceful Shutdown** 로직이 담긴 `deploy.sh` 모의 설계 완료.

### [🚀 현재 단계] Phase 5: 실전! AWS 실서버 파이프라인 구축 (Next Step)
- **목표:** 시뮬레이션으로 짜둔 로직을 실제 AWS 자원(EC2)과 GitHub Actions에 연결하여 눈으로 다운타임 0초를 확인한다.
- **실습 예정 내역:**
  1. GitHub Actions 워크플로우(`.yml`) 실제 작성 및 트리거 테스트.
  2. EC2에 직접 접속하여 Nginx 라우팅 스위칭이 실제로 작동하는지 포트 모니터링.
  3. 모든 연동이 완료된 후 "진짜 팩트 기반"의 무중단 배포 기술 블로그 최종 렌더링.

---

### 💡 새 채팅창을 연 후의 첫 번째 액션
이 워크스페이스에서 AI 튜터를 다시 부르신 후, **"Phase 5 실전 파이프라인 구축 시작하자"** 라고 말씀해 주시면, 제가 즉시 이 문서를 스캔하고 GitHub Actions 연동부터 압박 질문을 던지며 훈련을 시작하겠습니다! 😈
